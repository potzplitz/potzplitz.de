Detailseite für level
Account bearbeiten
dropdown wenn auf login klick

level detail
leaderboard sort by submit date (default), attempts (, progress, erst möglich wenn spielstände eingelesen werden können)
-> verknüpfungen zu anderen leveln (zb practice copies) können hergestellt werden und so attempts summiert werden
-> feature für liste an custom attempt einträgen, auswählbar zwischen +/- (was attempts addiert/subtrahiert)
-> liedervorschau wie auf osu
-> verification button

allgemein
verknüpfung zwischen gd account und page account (von colons seite abschauen)
    => WICHTIG: mit möglichkeit zum daten speichern (LOCAL STORAGE, ODER VERSCHLÜSSELT IN DER DB) oder nicht
    => Möglichkeit für Kommentare / Mails verfassen über GD Account/API
    => Irgendwie es schaffen, level download zu ermöglichen

Error Pages
klasse erstellen für error pages
$ErrorPage->show(<http error code>)
 => route auf diese page

sicherheit:

adminpanel:
admin audit log (wer hat was geändert)

brute force schutz
login endpunkt unbegrenzt => lockout machen

#PRIOS

2. Graues Standardbild
3. Profil Bearbeiten
4. Admin Panels
5. Sicherheit

